import 'package:flutter/material.dart';

class ClassReservation extends StatelessWidget {
  const ClassReservation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
